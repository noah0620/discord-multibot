# チケット作成ログの設定

1. BOTを停止し、既存の `.env` と `data/store.json` をバックアップしてください。
2. 修正版の `src` を既存のBOTフォルダーへ上書きしてください。ZIPに `.env` と `data/store.json` は含まれません。
3. `npm install` → `npm run deploy-commands` → `npm start` を実行してください。
4. Discordで `/ticket-settings log_channel:#ログ用チャンネル` を実行してください。カテゴリ・サポートロールは必要な場合のみ指定します。
5. `/ticket-status` で作成ログの設定を確認し、`/ticket-panel` から実際にチケットを1件作成してログが届くか確認してください。

ログにはチケット番号、チケットチャンネル、作成者、ユーザーID、作成日時が記録されます。チケット作成後のログ送信に失敗した場合もチケットは残り、操作したユーザーに警告を表示してBOTコンソールにエラーを出します。ログ送信済みメッセージIDもチケットデータに保存します。BOTにはログチャンネルで「チャンネルを見る」「メッセージを送信」「埋め込みリンク」の権限が必要です。
