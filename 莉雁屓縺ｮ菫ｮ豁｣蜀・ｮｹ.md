# 今回の修正内容

## 天気予報 自動投稿の安定化
- 投稿先チャンネルごとに当日の成功状態を保存します。
- 1チャンネルだけ失敗しても、成功済みチャンネルを重複投稿せず、失敗したチャンネルだけ次回監視（約15秒後）で再試行します。
- 全投稿先が成功した時だけ、その日の自動投稿を完了扱いにします。
- `/weather-setup` をやり直した際は、チャンネル別の送信済み状態もリセットします。
- PowerShellログに「完了」「再試行待ち」「チャンネル別エラー」を表示します。

## チケット作成ログ
`/ticket-settings` に `log_channel` を追加しました。

例:
`/ticket-settings category:<カテゴリ> support_role:<サポート> log_channel:<ログチャンネル>`

チケット作成時、指定チャンネルへ以下を投稿します。
- チケット番号 / チケットチャンネル
- 作成者メンション / DiscordユーザーID
- 作成日時

`/ticket-status` でも作成ログの設定先を確認できます。

## 導入
既存の `.env` と `data/store.json` は引き継いでください。

```powershell
npm install
npm run deploy-commands
npm start
```

`ticket-settings` のオプションが増えているため `npm run deploy-commands` は必須です。
