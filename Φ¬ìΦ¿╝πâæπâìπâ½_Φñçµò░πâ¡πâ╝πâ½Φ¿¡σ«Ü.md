# 認証パネル：複数ロール設定

`/verify-panel` の `role` で最初のロールを選び、`additional_roles` に追加のロールIDまたはロールメンションをカンマ区切りで入力します（1パネル最大20ロール）。例: `123456789012345678, 234567890123456789`。

管理者が申請を承認すると、そのパネルに設定されたロールをまとめて付与します。別の認証パネルには別のロールセットを設定できます。古い単一ロールの認証パネル・申請も引き続き扱います。

導入時は既存の `.env` と `data` を保持してください。`npm install` → `npm run deploy-commands` → `npm start` を実行します。BOTのロール管理権限とロール階層が必要です。実サーバーでの動作確認は未実施です。
